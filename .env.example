ANTHROPIC_API_KEY=sk-ant-example
COACH_MODEL=claude-3-5-sonnet-20241022
# Optional: Lichess OAuth token (https://lichess.org/account/oauth/token).
# The opening explorer API now requires auth; without it the app falls back
# to offline data. Tablebase needs no token.
LICHESS_EXPLORER_TOKEN=
